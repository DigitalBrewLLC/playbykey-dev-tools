---
title: Scales
description: Compute blues scale, pentatonic, harmonic minor, and derived scale note sets in @playbykey/theory
---

import ScalesPlayground from '../../../components/playgrounds/ScalesPlayground';

The scales module computes note sets for derived scale types — blues scale, pentatonic, harmonic minor, and other patterns beyond the seven diatonic modes. Use these functions when you need concrete note arrays for non-modal scale types or scale metadata like emphasis degrees.

## Live Playground

<ScalesPlayground client:load />

## Function Reference

**Note arrays:** `getBluesNotes`, `getHarmonicMinorNotes`, and `getDerivedScaleNotes` return concrete note lists for a root (and scale kind where applicable).

**Context and degrees:** `getScaleContextNotes` returns the parent diatonic context for a derived scale. `getPentatonicDegrees` and `getScaleEmphasisDegrees` return scale-degree numbers rather than note names.

**Utility:** `notesFromSemitoneOffsets` builds a custom note array from semitone offsets — useful when defining your own scale patterns programmatically.